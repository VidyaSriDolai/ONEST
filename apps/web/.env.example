# Base URL for API calls. Left empty in development so requests go through the
# Vite proxy defined in vite.config.ts and stay same-origin.
VITE_API_BASE_URL=

# Canonical public origin, used for SEO tags and sitemap URLs.
VITE_SITE_URL=http://localhost:5173
